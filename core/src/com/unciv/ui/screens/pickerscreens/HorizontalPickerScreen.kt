package com.unciv.ui.screens.pickerscreens

/**
 * Picker screen that aligns the buttons of the right side group horizontally instead of vertically
 */
open class HorizontalPickerScreen(disableScroll: Boolean = false): PickerScreen(disableScroll = disableScroll, horizontally = true)
